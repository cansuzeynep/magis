name: Rental Monitor

on:
  schedule:
    # Runs every 5 minutes
    - cron: '*/5 * * * *'
  workflow_dispatch:  # Allows manual trigger from GitHub UI
    inputs:
      clear_cache:
        description: 'Clear the seen listings cache'
        required: false
        type: boolean
        default: false

jobs:
  check-rentals:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      
      - name: Install dependencies
        run: pip install requests beautifulsoup4
      
      - name: Cache seen listings
        uses: actions/cache@v4
        with:
          path: seen_listings.json
          key: seen-listings-${{ github.run_id }}
          restore-keys: |
            seen-listings-
      
      - name: Run rental monitor
        env:
          SENDER_EMAIL: ${{ secrets.SENDER_EMAIL }}
          SENDER_PASSWORD: ${{ secrets.SENDER_PASSWORD }}
          RECIPIENT_EMAIL: ${{ secrets.RECIPIENT_EMAIL }}
          CLEAR_CACHE: ${{ github.event.inputs.clear_cache }}
        run: python rental_monitor.py
      
      # The cache is automatically updated by actions/cache at the end of the run if it successfully completes.
